<%= simple_form_for(@service) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :is_complete %>
    <%= f.input :protocol %>
    <%= f.input :proof %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
