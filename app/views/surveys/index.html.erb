<%- model_class = Survey -%>
<div class="page-header">
  <h1><%=t '.title', :default => Survey %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:has_cracks) %></th>
      <th><%= model_class.human_attribute_name(:has_maintanance_sticker) %></th>
      <th><%= model_class.human_attribute_name(:has_damp_areas) %></th>
      <th><%= model_class.human_attribute_name(:has_fire_extinguisher) %></th>
      <th><%= model_class.human_attribute_name(:has_maintanance_and_inspection_manuel) %></th>
      <th><%= model_class.human_attribute_name(:has_fire_wall) %></th>
      <th><%= model_class.human_attribute_name(:has_free_evacuation_route) %></th>
      <th><%= model_class.human_attribute_name(:has_closeable_fire_doors) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @surveys.each do |survey| %>
      <tr>
        <td><%= survey.id %></td>
        <td><%= survey.has_cracks %></td>
        <td><%= survey.has_maintanance_sticker %></td>
        <td><%= survey.has_damp_areas %></td>
        <td><%= survey.has_fire_extinguisher %></td>
        <td><%= survey.has_maintanance_and_inspection_manuel %></td>
        <td><%= survey.has_fire_wall %></td>
        <td><%= survey.has_free_evacuation_route %></td>
        <td><%= survey.has_closeable_fire_doors %></td>
        <td><%=l survey.created_at, format: :long %></td>
        <td>
          <%= link_to survey_path(survey), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to survey_path(survey), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_survey_path,
            :class => 'btn btn-primary' %>
